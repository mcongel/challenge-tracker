# Frontend (Vite) — Sackets Supabase project, anon key is safe in the browser.
VITE_SUPABASE_URL=https://mlvntnbgboinjhmavwao.supabase.co
VITE_SUPABASE_ANON_KEY=

# Seed script only (scripts/seed.mjs) — service role key, NEVER in the browser
# and never committed. Set these in the shell, not in .env files that ship.
SUPABASE_URL=https://mlvntnbgboinjhmavwao.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
