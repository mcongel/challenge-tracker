{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"]
  },
  // The engine/alerts imports come in transitively — listing them here would
  // only re-check the test suite a second time for zero extra coverage.
  "include": ["scripts/**/*.ts"]
}
